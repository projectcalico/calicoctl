general:
  artifacts:
    - "dist"
machine:
  services:
    - docker
test:
  override:
    - make release/calicoctl test-containerized
experimental:
  notify:
    branches:
      only:
        - master
